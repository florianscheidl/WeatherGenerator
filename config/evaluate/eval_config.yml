#optional: if commented out all is taken care of by the default settings
# NB. global options apply to all run_ids 

#global_plotting_options:
#  regions: ["europe", "global"] # Have regions here, if you want for them to apply to all streams (map generation)
#  image_format : "png" #options: "png", "pdf", "svg", "eps", "jpg" ..
#  animation_format: "gif" #options: "mp4", "gif"
#  dpi_val : 300
#  fps: 2
#  n_bins: 50 #number of bins for histograms. 
#  log_x: true #use log scale for x axis in histograms. 
#  log_y: true #use log scale for y axis in histograms. 
#  ERA5:
#    use_datashader: false
#    marker_size: 2
#    scale_marker_size: 1
#    marker: "o"
#    add_healpix_grid: false
#    healpix_nside: 4
#    # alpha: 0.5
#    2t: 
#      vmin: 250
#      vmax: 300
#    10u:
#      vmin: -40
#      vmax: 40


# max_workers: 36  # hard cap on parallel workers (I/O, plotting, scoring)

evaluation:
  metrics  : ["rmse"]
  # regions: ["global", "nhem"] # Have regions here, if you want for them to apply to all streams (scores calculation)

  # ── Score visualisations (new list-based config) ──
  # Supported values: lead_time, ratio, heatmap, scorecard, bar, qq,
  #                   rank_histogram, score_map, score_animation, timeseries
  # NOTE: PSD plots are produced automatically when psd is in metrics.
  score_plots: [lead_time, timeseries]

  # Legacy boolean flags (deprecated — use score_plots list above instead):
  # summary_plots : true
  # ratio_plots : false
  # heat_maps : false
  # score_cards: false
  # bar_plots: false
  # plot_score_maps: false
  # plot_score_animations: false
  # plot_score_init_timeseries: true

  summary_dir: "./plots/"
  plot_ensemble: "members" #supported: false, "std", "minmax", "members"
  plot_score_maps: false #plot scores on a 2D maps. it slows down score computation 
  print_summary: false #print out score values on screen. it can be verbose
  log_scale: false
  add_grid: false
  score_cards: false
  bar_plots: false
  num_processes: 0  #options: int, "auto", 0 means no parallelism (default)
  # baseline: "ar40mckx"


default_streams:
  ERA5:
    regions: ["global"]
    channels: ["2t", "10u", "10v", "z_500", "t_850", "u_850", "v_850", "q_850", "q_200", "q_300", "q_500", "q_700", "q_925", "q_1000"]
    evaluation:
      forecast_step: "all"
      sample: "all"
      ensemble: "all" #supported: "all", "mean", [0,1,2]
    plotting:
      sample: [1, 3]
      forecast_step: [1,3, 2] #supported: "all", [1,2,3,...], "1-50" (equivalent of [1,2,3,...50])
      ensemble: [0,2,5] #supported: "all", "mean", [0,1,2]
      plot_maps: true
      plot_bias: false
      plot_target: false
      plot_histograms: true
      plot_animations: true
      
run_ids :
  dau1i8bl:
    label: "pretrained model dau1i8bl"
    results_base_dir : "./results/dau1i8bl/"
    #NEW: if "streams" is not specified, the default streams are used
    
      
  # c8g5katp:
  #   label: "2 steps window"
  #   results_base_dir : "./results/"
  #   #NEW: if "streams" is not specified, the default streams are used